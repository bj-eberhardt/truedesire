# Repository Instructions

- Do not use PowerShell for text/code replacements. Use Node.js or Python scripts for edits that replace text so UTF-8 and German umlauts are preserved correctly.
- Do not add standalone architecture check scripts to package scripts. Architecture rules must be implemented as normal tests alongside the existing architecture tests.
